package br.com.fga.mock;

import java.util.List;

import br.com.fga.models.Simulation;

public class SimulatorData {

    public static final List<Simulation> SIMULATION_LIST = List.of(
        // new Simulation(new Position(0.0, 0.0), VehicleData.get(0)),
        // new Simulation(new Position(10.0, 30.0), VehicleData.get(1)),
        // new Simulation(new Position(56.0, 80.0), VehicleData.get(2))
    );

}
